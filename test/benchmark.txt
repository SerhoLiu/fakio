测试环境：
    1. Ubuntu 12.04 x86_64
    2. CPU: Intel(R) Core(TM)2 Duo CPU P7450  @ 2.13GHz
    3. 内存: 4016236 kB

测试网站：本地使用 godoc -http=:6060

测试说明：
    1. ./fbench -c <clients> -n <requests> url
    2. 上面加上 -f 参数就会使用 Fakio 访问，通过和直接访问的结果进行对比，可以得出结果
    3. fakio-server 中对 context_list, event_loop 大小的设置
       会对结果产生影响，如果并发数大的话，需要调大 connections
    4. sudo sh -c "ulimit -n 65535 && exec su $LOGNAME"

测试用例以及相应结果:
    
    1. GET http://localhost:8000/ 首页大小为 26.0Kb

       i. -t 30 -c 200:
            Speed=76476 pages/min, 33289404 bytes/sec.
            Requests: 38238 susceed, 0 failed.
      ii. -t 30 -c 1000:
            Speed=72876 pages/min, 31239700 bytes/sec.
            Requests: 35979 susceed, 459 failed.

    2. GET http://localhost:8000/1.tar.gz 大小为 26.6Mb
       i. -t 30 -c 200:
            Speed=34006 pages/min, 43638320 bytes/sec.
            Requests: 17003 susceed, 0 failed.
      ii. -t 30 -c 100:
            因为文件大，所以测试是需要调整很多系统参数，而且 Web 服务器已经不堪重负，
            换一个服务器再测